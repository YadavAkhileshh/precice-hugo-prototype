# preCICE Hugo Prototype

Hugo site with Docsy theme, dark mode toggle, and Pagefind search.

## Local Development

```bash
npm install
npx hugo-extended server
```

Open http://localhost:1313

## Build

```bash
npx hugo-extended --gc --minify
```

## Deploy

Connected to Netlify. Pushes to `main` branch auto-deploy.
