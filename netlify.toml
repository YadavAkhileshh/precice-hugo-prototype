[build]
  command = "npm install && npx hugo-extended --gc --minify && npx pagefind --site public"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.156.0"
  HUGO_EXTENDED = "true"
  NODE_VERSION = "18"
